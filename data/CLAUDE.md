# Data layout

The per-language data layout and pipeline live in [AGENTS.md](AGENTS.md) (kept
there so non-Claude agents read them too). Each language's editing conventions
live with its own tree (e.g. `data/japanese/AGENTS.md`). Follow those files.

@AGENTS.md
